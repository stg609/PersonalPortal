{
  "Name": "PostService",
  "IdentityUrl": "http://localhost:5000",
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConsulConfig": {
    "Address": "http://localhost:8500" //必须要合法的 url（schema://host:port），写成 localhost:8500 则没有用
  }
}
